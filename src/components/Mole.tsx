import { useSelector, useDispatch } from "react-redux";
import { SCORE, SHOW_POP_UP } from "../actions/types";
import { storeState } from "../lib/gameTypes";

const Mole = () => {
  return <div></div>;
};
export default Mole;
